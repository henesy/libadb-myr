# A record with 4 tuples and 5 pairs
type=person
	name='Ана'
	age=10
    food='P is for potato' type="starch"

# A record with 4 tuples of 1 pair each
quoting=
    quot='foo '' bar'
    dquot="foo ' bar"
    equot='foo " bar'

# A record consisting of a single tuple and 4 pairs
type=rectangle height=4 width=6		name='A'

# A record consisting of 3 tuples and 4 pairs
type=rectangle name='B'
    height=9
	width=7
